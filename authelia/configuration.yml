---
# Authelia Configuration for MSP DokuTool
# For production: replace secrets and use trusted certificates

server:
  address: 'tcp://:9091'

log:
  level: info

totp:
  issuer: 'MSP DokuTool'

identity_validation:
  reset_password:
    jwt_secret: 'mspdoku_jwt_secret_change_in_production_1234567890'

authentication_backend:
  file:
    path: /config/users.yml
    password:
      algorithm: bcrypt

session:
  secret: 'mspdoku_session_secret_change_in_production_1234'
  cookies:
    - domain: '127.0.0.1'
      authelia_url: 'https://127.0.0.1:3443/.authelia'
      default_redirection_url: 'https://127.0.0.1:3443'
      name: 'authelia_session'
      same_site: 'lax'
      inactivity: '5m'
      expiration: '1h'

storage:
  encryption_key: 'mspdoku_storage_encryption_key_change_in_prod_1234'
  local:
    path: /config/db.sqlite3

notifier:
  filesystem:
    filename: /config/notification.txt

access_control:
  default_policy: deny
  rules:
    # Static assets — no auth
    - domain: '127.0.0.1'
      resources:
        - '^\.(js|css|png|svg|ico|woff|woff2|ttf)$'
      policy: bypass

    # Admins and technicians — one factor only
    - domain: '127.0.0.1'
      subject:
        - 'group:admins'
        - 'group:technicians'
      policy: one_factor

    # Tenant users — require two factor (TOTP)
    - domain: '127.0.0.1'
      policy: two_factor
