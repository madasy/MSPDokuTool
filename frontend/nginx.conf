# Redirect HTTP to HTTPS
server {
    listen 80;
    server_name localhost;
    return 301 https://$host:3443$request_uri;
}

server {
    listen 443 ssl;
    server_name localhost 127.0.0.1;

    ssl_certificate /etc/nginx/certs/server.crt;
    ssl_certificate_key /etc/nginx/certs/server.key;

    root /usr/share/nginx/html;
    index index.html;

    resolver 127.0.0.11 valid=30s;
    set $upstream_authelia http://authelia:9091;

    # Internal Authelia verification subrequest
    location /authelia-verify {
        internal;
        proxy_pass $upstream_authelia/api/verify?auth=basic;

        proxy_set_header Content-Length "";
        proxy_set_header Connection "";
        proxy_pass_request_body off;

        proxy_set_header X-Original-Method $request_method;
        proxy_set_header X-Original-URL $scheme://$http_host$request_uri;
        proxy_set_header X-Forwarded-For $remote_addr;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $http_host;
        proxy_set_header X-Forwarded-URI $request_uri;

        # Tell Authelia where to redirect for login
        proxy_set_header X-Authelia-URL https://$http_host/.authelia;
    }

    # Authelia portal — proxied through nginx with HTTPS
    location /.authelia/ {
        proxy_pass $upstream_authelia/;
        proxy_set_header Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $http_host;
    }

    # API requests — protected, user headers forwarded to backend
    location /api/ {
        auth_request /authelia-verify;
        auth_request_set $user $upstream_http_remote_user;
        auth_request_set $groups $upstream_http_remote_groups;
        auth_request_set $name $upstream_http_remote_name;
        auth_request_set $email $upstream_http_remote_email;

        # On 401 redirect to Authelia login
        error_page 401 =302 https://$http_host/.authelia/#/?rd=$scheme://$http_host$request_uri;

        proxy_pass http://backend:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Remote-User $user;
        proxy_set_header Remote-Groups $groups;
        proxy_set_header Remote-Name $name;
        proxy_set_header Remote-Email $email;
    }

    # SPA — protected by Authelia
    location / {
        auth_request /authelia-verify;
        error_page 401 =302 https://$http_host/.authelia/#/?rd=$scheme://$http_host$request_uri;
        try_files $uri $uri/ /index.html;
    }

    # Static assets — no auth needed (must be BEFORE the / block)
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|map)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
